# Codex environment record

This record describes the Phase 1 documentation environment. It is not a numerical benchmark environment.

| Field | Observed value |
| --- | --- |
| UTC capture time | `2026-09-04T16:17:04Z` |
| Operating system | macOS 26.4, build 25E246 |
| Architecture | arm64 |
| Git | 2.50.1 (Apple Git-155) |
| GitHub CLI | 2.98.0 |
| Julia | 1.12.7 |
| Quantum ESPRESSO | Executable detected, but version query could not initialize MPI in the restricted environment; pending verification |
| Codex model | OpenAI Codex; exact model identifier not exposed to the task |

The workbench repository was public and writable at pre-flight. Its `main` branch had no commit. The configured GitHub profile exposed the login `xxs4661` but no legal name, so Phase 1 uses the login as the MIT copyright holder.

No authentication output is stored in this repository.
