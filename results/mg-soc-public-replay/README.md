# Phase 7G: public cross-environment replay — BLOCKED

GitHub rejected the first branch push because the existing OAuth credential lacks
`workflow` scope. **No Linux job, frozen R process or independent I process ran.**
The implementation and 47 passing Mac synthetic tests are available locally;
they do not establish real-data integration or cross-platform reproducibility.
There are no new T, NL, density, local-integral, J or ledger differences to report.

The authoritative small records are [replay.json](replay.json) and
[evidence.json](evidence.json). Original scientific results remain unchanged.

## Commits and actual authorization failure

| Role | SHA / state |
| --- | --- |
| Accepted public scientific/input base | `747d7d3e21dadd8a5fef0183d2375e7f1f82c176` |
| Committed preparation, environment and gates | `757397ab0f0080f783d64841a6522db7f7d851b0` |
| Committed execution candidate, scripts and workflow | `95334efbab69fe97006ac76cbf4d676d4a7efd9f` |
| Actual Linux execution / tested commit | **NOT_RUN** / none |
| Delivery | Ordinary subsequent documentation/receipt commit; see Git history. No CI claim for this commit. |
| Branch | `codex/phase7g-public-cross-env-replay`, local only |

The only push attempted was:

```sh
git push --set-upstream origin codex/phase7g-public-cross-env-replay
```

Exit **1**; GitHub's reason was “refusing to allow an OAuth App to create or update
workflow `.github/workflows/phase7g-public-replay.yml` without `workflow` scope”.
The captured original output is archived as `process/push-attempt.json`; it was
not generated by a second attempt. Read-only `GET /user` confirmed scopes
`gist, read:org, repo` at 2026-09-08 11:59:24 UTC. Scope refresh, administrator
bypass and permissions/settings changes were **NOT_REQUESTED**.

The repository is public and its Actions are enabled; the blocker is permission
to publish the new workflow. An exact-head Actions query returned **0 runs** for
the candidate. There is no run ID, job ID, attempt number or numerical exit code
to manufacture. The remote task branch is absent. Remote main before and after
rejection remains `77bd2e151116bcc189b797e6da415d27a61d3a2b`.
No existing different-OS/CPU runner was identified; no replacement resource was
created. No additional push is useful without a separate authorization change.

## Completed preparation and tests

The [committed plan](../../benchmarks/mg-soc-public-replay-v1/plan.json) binds
**59 public files, 15,876,489 bytes**, to exact accepted-base Git blobs and
SHA-256. That byte check passed on the preparation host. NPZ member validation,
the three in-memory original WFC reconstructions, and all real numerical gates
are **NOT_RUN**. A file-hash check is not their substitute.

The declared ephemeral target is standard Ubuntu 24.04 x86_64, CPython 3.12.14
and the exact hash-locked NumPy 2.3.5 manylinux wheel. Official Actions full
commit SHAs and the wheel were verified before the preparation commit.
The workflow limits execution to this branch, one job, read-only repository
permission and a detached read-only input tree. It records the actual build,
library hashes and BLAS thread count if executed. Those Linux facts have **not**
been measured. No dependency was installed or changed on the Mac.

Actual synthetic host: **macOS 26.6.2 arm64**, existing CPython **3.12.14**,
NumPy **2.3.5**. All commands below exited **0**, with bytecode writes disabled:

| Command | Synthetic tests |
| --- | ---: |
| `python tests/test_independent_orbital_io.py` | 9 |
| `python tests/test_independent_orbital_math.py` | 14 |
| `python tests/test_independent_replay.py` | 7 |
| `python tests/test_phase7g_observer.py` | 7 |
| `python tests/test_cross_env_replay.py` | 10 |
| Total | **47** |

Per-suite stdout/stderr hashes and archive-relative log paths appear in
`replay.json`. The input-reader tests cover malformed packages and original-byte
mutation; math tests cover nonzero k/tilted cells, complex density, full D and
spin interference, all bands, integer remapping and correlated print intervals.
Protocol tests preserve failures and reject stale outputs. Initial wrapper red
tests exposed nonfinite equality, malformed hash exemptions and missing exit-code
acceptance; their log is retained and assertions were not weakened. Initial
synthetic interpreter/path issues are separately disclosed in the receipt.

Publication path/privacy/relative-link checks passed for all current public files;
the 23 required entries and 86 frozen scientific file hashes were verified.
The old full Python 3.9 numerical publication chain was **NOT_RUN**, as requested
for this phase. These checks do not invoke old R, Julia, UPF or a physical solver.
Whitespace and final evidence/code bindings are checked before delivery.

## Prepared paths and independence limits

R preserves the unmodified `python scripts/check_orbital_energy.py` entry without
`--all`. An opt-in return observer saves already computed values to diagnose a
failure without replacing functions or suppressing exceptions. Raw R exit and
output remain authoritative. No real R failure has occurred this phase.

I has separate bounded readers and streamed NumPy formulas; it never imports or
calls the production parse/density/kinetic/projection/comparison helpers. Its
planned Q scope includes every original coefficient, all 72 states, all saved
density modes, 17 direct correlations, kinetic sum/gradient checks, published
P/D action, P2 integration and the signed ledger. A/B remain
**SCALAR_AND_PROJECTED_ONLY**, with no public original A/B orbitals.

The mathematical implementation and synthetic tests preceded target-value
comparisons. During public-format reconnaissance, the IO author incidentally
saw summary energies in the old README before the reader tests; those values
were not used to derive results or fit constraints. The paths share public
data/P/D, NumPy and potentially FFT/BLAS components. This is AI-assisted code-path
independence, not another DFT code, independently generated P/D or human expert
certification; see [AI disclosure](../../docs/ai-assistance.md).

Only the two predeclared derived density hashes can explain a future exact-byte
platform failure, after all required numerical checks and the raw R reason are
bound. NumPy-version mismatch, input hashes, P2 token-derived hashes, semantic
fields or numerical failures are not exempt. A successful I cannot relabel R or
the workflow as PASS. These classifications are **PREDECLARED_NOT_TESTED**.
Original Mac node arrays are not public; their nodewise differences cannot be
inferred from historical hashes.

## Status and archive

| Dimension | Actual status |
| --- | --- |
| Public input identity | FILE_HASH_VERIFIED_ONLY; members/WFC NOT_RUN |
| Different environment | BLOCKED |
| Frozen R / independent I | NOT_RUN / NOT_RUN; both exit codes null |
| Full Q public numerical replay | NOT_RUN |
| A/B scope / replay | SCALAR_AND_PROJECTED_ONLY / NOT_RUN |
| Historical platform fingerprints | PREDECLARED_NOT_TESTED |
| Numerical interpretation | REVIEW_REQUIRED |
| Native QE nonlocal energy | NOT_MEASURED |
| Original Q input-H residual | NOT_AVAILABLE |
| Physical convergence / IEEE origin | NOT_ESTABLISHED / NOT_LOCALIZED |

External archive `phase7g-public-replay-20260908-blocked` contains this phase's
process logs and a Git bundle for the two new preparation/candidate commits,
with the public base as prerequisite. Bundle verification and restoration into
a new temporary Git repository succeeded; all **14 new committed files** matched
the candidate. All **41** archived process files were restored from the archive
and matched their manifest hashes. Archive IDs, bundle/tar hashes and scope are
in the receipts. The final ordinary delivery commit is appended to the local
archive after commit; no self-referential SHA or amend is required. No old private
archive or scientific working directory was read, and old numeric packages are
referenced rather than copied into this public result directory.

All new SCF, eigen/occupation solves, QE/pp, Julia science, XC, regenerated P/D,
physical scans and complete upstream physical suites are **NOT_RUN**. The Linux
replay remains unfinished; no recommendation that its numerical gates passed
can be made. Stop here for human review, without expanding permissions or
starting another scientific stage.
