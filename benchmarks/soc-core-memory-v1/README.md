# SOC core memory work — frozen Phase 9A preparation

[Plan](plan.json), [source identities](sources.json) and [numerical/performance contract](contract.json) precede the fixed-base reference measurements and every implementation change.

The four static suites use unchanged complete historical B0/K4 arrays. Each operation has one warmup and five retained samples. Only after all static gates pass are two once-only new B0 tasks authorized. Existing algorithms, dependencies, inputs and historical results stay frozen. Current results and execution receipts will be separate; this preparation is not a PASS claim.
